services:
  kcap:
    build:
      context: .
    image: kcap:local
    init: true
    ports:
      - "8100:8100"
    environment:
      KCAP_CORS_ORIGINS: "http://localhost:8100"
    read_only: true
    tmpfs:
      - /tmp:size=16m
    security_opt:
      - no-new-privileges:true
