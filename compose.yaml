services:
  kcap:
    build:
      context: .
    image: kcap:local
    init: true
    ports:
      - "8000:8000"
    environment:
      KCAP_CORS_ORIGINS: "http://localhost:8000"
    read_only: true
    tmpfs:
      - /tmp:size=16m
    security_opt:
      - no-new-privileges:true
